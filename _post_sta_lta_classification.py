# It would set ``sta_window``, ``lta_window`` and ``threshold`` to acheive high false positive.
# Then, we save all positives to templates and extract the feature or pattern. 
# Then, we apply clustering or classification to distinguish false positive and true positive. 